import React from 'react';
import {
    Layout,
} from 'antd';

const {
    Header,
} = Layout;

const HeadBar = () => (
    <Header style={{ background: '#fff', padding: 0 }}>
        <p>this is header</p>
    </Header>
);

export default HeadBar;
