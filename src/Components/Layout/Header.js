import React from 'react';
import {
    Layout,
} from 'antd';

const {
    Header,
} = Layout;

const HeadBar = () => (
    <Header style={{ background: '#fff', padding: 0 }}>
        this is header
    </Header>
);

export default HeadBar;
